[project]
name = "cat-captions-visual"
version = "0.1.0"
description = "descrições visuais das fotos da Duda"
requires-python = ">=3.11"
dependencies = [
    "google-genai",
    "pillow>=12.2.0",
]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
