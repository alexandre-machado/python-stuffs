[project]
name = "multiprocessing-examples"
version = "0.1.0"
description = "Exemplos de multiprocessing com tqdm: imap, imap_unordered e apply_async"
requires-python = ">=3.11"
dependencies = ["tqdm"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
