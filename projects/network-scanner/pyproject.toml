[project]
name = "network-scanner"
version = "0.1.0"
description = "Varre a sub-rede 192.168.0.0/24 procurando hosts com a porta 554 (RTSP) aberta"
requires-python = ">=3.11"
dependencies = ["tqdm"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
