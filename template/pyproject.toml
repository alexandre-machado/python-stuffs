[project]
name = "{{NAME}}"
version = "0.1.0"
description = "{{DESC}}"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
